import React from 'react';
import { Button } from '@material-ui/core';

const Header = () => {
  return (
    <div styles={{ padding: 100 }}>
      <Button />
    </div>
  );
};

export default Header;
