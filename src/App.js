import React from "react";
import { Button } from "@material-ui/core";

const App = (props) => {
  return (
    <Button variant="outlined" color="primary">
      This is my Button
    </Button>
  );
};

export default App;
