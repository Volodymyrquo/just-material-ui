import React from 'react';
import { AppButtons, AppHeader, AppList } from './components';

const App = (props) => {
  return (
    <>
      <AppHeader />
      <AppButtons />
      <AppList />
    </>
  );
};

export default App;
